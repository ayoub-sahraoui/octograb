# Development environment variables
# Copy this file to .env and set VITE_DEV_MODE=true for local development
# DO NOT commit .env files to git!

# Enable dev mode bypass for license checks (development only)
# REMOVE or set to false before building for production!
VITE_DEV_MODE=false
